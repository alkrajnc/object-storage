# Used for the postgresql database
DOCKER_CONTAINER_NAME = "container_name"

# Path to the object storage
STORAGE_PATH = "/some/path/to/storage"

# Used for the ExpressJS session secret
# npr. openssl rand -base64 32
SESSION_SECRET = ""

DB_HOST = ""
DB_USER = ""
DB_PASSWORD = ""
DB_NAME = ""